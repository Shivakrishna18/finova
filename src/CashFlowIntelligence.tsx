import { useState } from 'react'
import PlasmaRing from './components/originkit/ui/plasma-ring'
import { demoFinancialState, formatINR } from './data/demoFinancialState'

type IncomeRecord = typeof demoFinancialState.incomeRecords[number]
type Commitment = typeof demoFinancialState.commitments[number]
const forecastDays = [30, 60, 90]

export default function CashFlowIntelligence() {
  const [incomeRecords, setIncomeRecords] = useState<IncomeRecord[]>(demoFinancialState.incomeRecords)
  const [commitments] = useState<Commitment[]>(demoFinancialState.commitments)
  const [forecastPeriod, setForecastPeriod] = useState(30)
  const [incomeModal, setIncomeModal] = useState(false)
  const [delayModal, setDelayModal] = useState(false)
  const [commitment, setCommitment] = useState<Commitment | null>(null)
  const [budgetValues, setBudgetValues] = useState(demoFinancialState.budgets)
  const [discretionary, setDiscretionary] = useState(0)
  const [scenario, setScenario] = useState('')
  const [incomeForm, setIncomeForm] = useState({ source: 'Freelance', amount: 10000, date: '2026-09-18', frequency: 'Monthly', status: 'EXPECTED' as IncomeRecord['status'] })
  const [delayDays, setDelayDays] = useState(10)
  const currentCommitments = commitments.reduce((sum, item) => sum + item.amount, 0)
  const goalAllocation = commitments.find(item => item.type === 'Goal allocation')?.amount ?? 0
  const trueAvailable = Math.max(0, demoFinancialState.balance - currentCommitments)
  const safeToSpend = Math.max(0, trueAvailable - goalAllocation - discretionary)
  const projectedBalance = trueAvailable + incomeRecords.filter(item => item.status === 'EXPECTED').reduce((sum, item) => sum + item.amount, 0) - demoFinancialState.monthlySpending
  const forecast = buildForecast(forecastPeriod, projectedBalance, demoFinancialState.monthlySpending, currentCommitments, goalAllocation)
  const velocity = demoFinancialState.monthlySpending / Math.max(1, demoFinancialState.income) * 100
  const incomeTotal = incomeRecords.reduce((sum, item) => sum + item.amount, 0)
  const addIncome = () => { if (!incomeForm.source || incomeForm.amount <= 0) return; const item: IncomeRecord = { id: `income-${Date.now()}`, source: incomeForm.source, amount: incomeForm.amount, expectedDate: incomeForm.date, frequency: incomeForm.frequency, status: incomeForm.status }; setIncomeRecords(previous => [...previous, item]); demoFinancialState.income += item.amount; demoFinancialState.balance += item.amount; demoFinancialState.safeToSpend += item.amount; setIncomeModal(false) }
  const delayIncome = (id: string) => { setIncomeRecords(previous => previous.map(item => item.id === id ? { ...item, status: 'DELAYED' } : item)); setDelayModal(false) }
  const navigate = (view: string) => window.dispatchEvent(new CustomEvent('finova-navigate', { detail: view }))
  return <div className="workspace-page cash-flow-page"><div className="page-intro"><div><span className="eyebrow">CASH FLOW / COMMAND CENTER</span><h1>Know what your money<br /><em>will do next.</em></h1><p>FINOVA looks beyond today's balance to understand the money coming in, going out, and what remains available.</p></div><span className="demo-badge">DEMO FINANCIAL STATE</span></div><div className="cash-snapshot">{[['CURRENT BALANCE', formatINR(demoFinancialState.balance)], ['MONTHLY INCOME', formatINR(demoFinancialState.income)], ['MONTHLY EXPENSES', formatINR(demoFinancialState.monthlySpending)], ['COMMITTED EXPENSES', formatINR(currentCommitments)], ['CURRENT SAVINGS', formatINR(demoFinancialState.income - demoFinancialState.monthlySpending)], ['SAFE TO SPEND', formatINR(safeToSpend)], ['PROJECTED MONTH-END', formatINR(projectedBalance)]].map(([label, value]) => <div key={label}><span>{label}<i>DEMO</i></span><strong>{value}</strong><small>Derived locally</small></div>)}</div><div className="cash-command-grid"><section className="income-timeline"><div className="card-top"><div><span className="panel-kicker">INCOME TIMELINE <i className="demo-dot" /> SIMULATED</span><h2>Money coming in</h2></div><button className="soft-button" onClick={() => setIncomeModal(true)}>＋ Add income source</button></div><div className="income-total">{formatINR(incomeTotal)} <small>expected and received in demo records</small></div>{incomeRecords.map(item => <div className="income-record" key={item.id}><div className={`income-status ${item.status.toLowerCase().replace(' ', '-')}`}><i />{item.status}</div><div><strong>{item.source}</strong><small>{item.frequency} · Expected {item.expectedDate}{item.actualDate ? ` · Received ${item.actualDate}` : ''}</small></div><b>{formatINR(item.amount)}</b>{item.status === 'EXPECTED' && <button onClick={() => setDelayModal(true)}>Delay</button>}</div>)}</section><section className="cash-core-panel"><div className="cash-core-scene"><div className="cash-orbit orbit-a" /><div className="cash-orbit orbit-b" /><div className="cash-core"><PlasmaRing background="rgba(0,0,0,0)" colors={['#73eaff', '#6677ff', '#b55dff']} density={42} speed={25} centerOpacity={8} scale={30} style={{ width: '100%', height: '100%' }} /><span>CASH<br /><small>FLOW CORE</small></span></div><span className="core-signal signal-income">INCOME</span><span className="core-signal signal-commitments">COMMITMENTS</span><span className="core-signal signal-goals">GOALS</span><span className="core-signal signal-safe">SAFE-TO-SPEND</span></div><div className="cash-core-label"><span>FINOVA FINANCIAL RADAR</span><strong>One state. Many signals.</strong></div></section></div><section className="forecast-panel"><div className="card-top"><div><span className="panel-kicker">CASH-FLOW FORECAST <i className="demo-dot" /> SIMULATION</span><h2>Projected balance path</h2></div><div className="forecast-tabs">{forecastDays.map(day => <button className={forecastPeriod === day ? 'active' : ''} onClick={() => setForecastPeriod(day)} key={day}>{day} DAYS</button>)}</div></div><div className="forecast-chart"><div className="forecast-axis"><span>₹{Math.round(forecast[0] / 1000)}k</span><span>₹{Math.round(forecast[Math.floor(forecast.length / 2)] / 1000)}k</span><span>₹{Math.round(forecast[forecast.length - 1] / 1000)}k</span></div><svg viewBox="0 0 700 220" preserveAspectRatio="none" role="img" aria-label={`Demo ${forecastPeriod} day cash flow forecast`}><polyline points={forecast.map((value, index) => `${index * (700 / (forecast.length - 1))},${210 - Math.min(180, Math.max(10, value / Math.max(projectedBalance, demoFinancialState.balance) * 180))}`).join(' ')} /></svg></div><div className="forecast-legend"><span><i /> Income: {formatINR(incomeRecords.filter(item => item.status !== 'DELAYED').reduce((sum, item) => sum + item.amount, 0))}</span><span><i /> Commitments: {formatINR(currentCommitments)}</span><span><i /> Goal allocation: {formatINR(goalAllocation)}</span></div></section><div className="commitment-grid"><section className="commitments-panel"><div className="card-top"><div><span className="panel-kicker">UPCOMING MONEY</span><h2>Committed money</h2></div><span className="demo-badge">SIMULATED</span></div><p className="panel-explanation">Money that appears available but is already needed for upcoming commitments.</p>{commitments.map(item => <button className="commitment-row" key={item.id} onClick={() => setCommitment(item)}><span className="commitment-dot" /><div><strong>{item.name}</strong><small>{item.type} · {item.date}</small></div><b>{formatINR(item.amount)}</b><span>↗</span></button>)}<div className="true-available"><span>ACCOUNT BALANCE</span><strong>{formatINR(demoFinancialState.balance)}</strong><span>TRUE AVAILABLE</span><strong>{formatINR(trueAvailable)}</strong></div></section><section className="safe-flow-panel"><span className="panel-kicker">SAFE-TO-SPEND / DEMO ENGINE</span><h2>{formatINR(safeToSpend)}</h2><p>Current available funds minus commitments, goal allocation and discretionary adjustment.</p><div className="safe-formula"><span>Balance <b>{formatINR(demoFinancialState.balance)}</b></span><span>Committed <b>− {formatINR(currentCommitments)}</b></span><span>Goal allocation <b>− {formatINR(goalAllocation)}</b></span><span>Discretionary <b>− {formatINR(discretionary)}</b></span></div><label>Adjust discretionary spending<output>{formatINR(discretionary)}</output><input aria-label="Adjust discretionary spending" type="range" min="0" max="15000" step="500" value={discretionary} onChange={event => setDiscretionary(Number(event.target.value))} /></label><strong className={safeToSpend > 10000 ? 'safe-state' : safeToSpend > 4000 ? 'caution-state' : 'avoid-state'}>{safeToSpend > 10000 ? 'SAFE' : safeToSpend > 4000 ? 'CONSIDER' : 'CAUTION'}</strong></section></div><div className="cash-lower-grid"><BudgetPlanner budgets={budgetValues} setBudgets={setBudgetValues} /><section className="velocity-panel"><span className="panel-kicker">SPENDING VELOCITY / SIMULATED</span><h2>How quickly capacity is moving</h2><div className="velocity-meter"><i style={{ width: `${Math.min(100, velocity)}%` }} /></div><div className="velocity-values"><span>Current pace <b>{Math.round(velocity)}%</b></span><span>Normal pace <b>45%</b></span><span>Projected month-end <b>{formatINR(demoFinancialState.monthlySpending + discretionary)}</b></span></div><strong className={velocity < 50 ? 'safe-state' : 'caution-state'}>{velocity < 50 ? 'BELOW NORMAL' : 'NORMAL'}</strong><div className="runway"><span>FINANCIAL RUNWAY / DEMO</span><b>{Math.max(1, Math.floor(trueAvailable / Math.max(1, demoFinancialState.monthlySpending / 30)))} days</b><small>If no new income arrives, illustrative available funds support current commitments at this pace.</small></div></section></div><section className="cash-signals"><div className="card-top"><div><span className="panel-kicker">FINOVA THINKING / SIMULATED AI EXPLANATION</span><h2>The state, explained simply.</h2></div></div><p>Your account balance is {formatINR(demoFinancialState.balance)}, but {formatINR(currentCommitments)} is already committed to upcoming expenses. Your true available balance is therefore {formatINR(trueAvailable)}.</p><div className="cash-signal-actions"><button onClick={() => setScenario('Income decrease 10%')}>What if income decreases 10%?</button><button onClick={() => setScenario('Expenses increase 15%')}>What if expenses increase 15%?</button><button onClick={() => setScenario('₹20,000 emergency expense')}>What if an emergency expense happens?</button><button onClick={() => navigate('What-If')}>Explore existing What-If ↗</button></div>{scenario && <div className="scenario-feedback">{scenario} · This would be evaluated through the existing demo scenario system.</div>}</section>{commitment && <CommitmentModal item={commitment} close={() => setCommitment(null)} />}{incomeModal && <IncomeModal form={incomeForm} setForm={setIncomeForm} submit={addIncome} close={() => setIncomeModal(false)} />}{delayModal && <DelayModal records={incomeRecords} days={delayDays} setDays={setDelayDays} confirm={delayIncome} close={() => setDelayModal(false)} />}</div>
}

function buildForecast(days: number, start: number, monthlyExpenses: number, commitments: number, goals: number) { const points = Math.max(3, Math.round(days / 15)); return Array.from({ length: points }, (_, index) => Math.max(0, start - index * ((monthlyExpenses + commitments + goals) / 4))) }
function BudgetPlanner({ budgets, setBudgets }: { budgets: Record<string, number>; setBudgets: (value: Record<string, number>) => void }) { const categories = Object.keys(budgets); return <section className="budget-planner"><div className="card-top"><div><span className="panel-kicker">MONTHLY BUDGET INTELLIGENCE</span><h2>Limits with context</h2></div><span className="demo-badge">DEMO</span></div>{categories.map(category => { const actual = demoFinancialState.transactions.filter(item => item.category.includes(category)).reduce((sum, item) => sum + item.amount, 0); const budget = budgets[category]; const status = actual > budget ? 'OVER BUDGET' : actual > budget * .75 ? 'WATCH' : 'ON TRACK'; return <div className="budget-row" key={category}><span>{category}</span><input aria-label={`${category} budget`} type="number" value={budget} onChange={event => setBudgets({ ...budgets, [category]: Number(event.target.value) })} /><b>{formatINR(actual)}</b><strong className={status === 'OVER BUDGET' ? 'over' : status === 'WATCH' ? 'watch' : ''}>{status}</strong><i><b style={{ width: `${Math.min(100, actual / Math.max(1, budget) * 100)}%` }} /></i></div> })}</section> }
function IncomeModal({ form, setForm, submit, close }: { form: { source: string; amount: number; date: string; frequency: string; status: IncomeRecord['status'] }; setForm: (value: { source: string; amount: number; date: string; frequency: string; status: IncomeRecord['status'] }) => void; submit: () => void; close: () => void }) { return <div className="modal-backdrop" onClick={close}><section className="modal cash-modal" onClick={event => event.stopPropagation()}><button className="modal-close" onClick={close} aria-label="Close income modal">×</button><span className="panel-kicker">ADD INCOME / DEMO DATA</span><h2>Add income source.</h2><label>Source name<input value={form.source} onChange={event => setForm({ ...form, source: event.target.value })} /></label><label>Amount<input type="number" value={form.amount} onChange={event => setForm({ ...form, amount: Number(event.target.value) })} /></label><label>Expected date<input type="date" value={form.date} onChange={event => setForm({ ...form, date: event.target.value })} /></label><label>Frequency<select value={form.frequency} onChange={event => setForm({ ...form, frequency: event.target.value })}><option>Monthly</option><option>Variable</option><option>One-time</option></select></label><label>Status<select value={form.status} onChange={event => setForm({ ...form, status: event.target.value as IncomeRecord['status'] })}><option>EXPECTED</option><option>RECEIVED</option><option>DELAYED</option><option>NOT RECEIVED</option></select></label><button className="soft-button" onClick={submit}>Add income source ↗</button></section></div> }
function DelayModal({ records, days, setDays, confirm, close }: { records: IncomeRecord[]; days: number; setDays: (value: number) => void; confirm: (id: string) => void; close: () => void }) { const income = records.find(item => item.status === 'EXPECTED'); return <div className="modal-backdrop" onClick={close}><section className="modal cash-modal" onClick={event => event.stopPropagation()}><button className="modal-close" onClick={close} aria-label="Close delay mode">×</button><span className="panel-kicker">INCOME DELAY MODE / SIMULATED FINANCIAL ANALYSIS</span><h2>Income delayed?</h2><p>Delaying this expected income reduces short-term safe-to-spend and may change the goal trajectory.</p><label>New expected date<input type="date" defaultValue="2026-09-25" /></label><label>Delay days<output>{days}</output><input type="range" min="1" max="30" value={days} onChange={event => setDays(Number(event.target.value))} /></label><div className="delay-impact"><span>BEFORE DELAY</span><strong>{formatINR(demoFinancialState.safeToSpend)}</strong><span>AFTER DELAY</span><strong>{formatINR(Math.max(0, demoFinancialState.safeToSpend - 3000))}</strong></div><button className="soft-button" onClick={() => income && confirm(income.id)}>Apply simulated delay ↗</button></section></div> }
function CommitmentModal({ item, close }: { item: Commitment; close: () => void }) { return <div className="modal-backdrop" onClick={close}><section className="modal cash-modal" onClick={event => event.stopPropagation()}><button className="modal-close" onClick={close} aria-label="Close commitment detail">×</button><span className="panel-kicker">UPCOMING MONEY / DEMO DATA</span><h2>{item.name}</h2><p>{formatINR(item.amount)} · {item.type} · expected {item.date}</p><p>Committed money is reserved in the illustrative cash-flow model so the true available balance stays visible.</p><button className="soft-button" onClick={close}>Close ↗</button></section></div> }
